/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    domains: ['blog-739442.assets.newt.so'],
    formats: ['image/webp'],
  },
};

export default nextConfig;
